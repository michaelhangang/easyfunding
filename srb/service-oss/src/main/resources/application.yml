server:
  port: 8130

spring:
  profiles:
    active: dev
  application:
    name: service-oss
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 # nacos
aliyun:
  oss:
    endpoint:
    keyId:
    keySecret: keysecret
    bucketName: srb-file
